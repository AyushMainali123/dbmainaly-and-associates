---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={`bg-surface-container-lowest rounded-xl border border-outline-variant/10 shadow-[0_20px_40px_rgba(0,35,111,0.02)] transition-all duration-300 ${className || ""}`}>
  <slot />
</div>
